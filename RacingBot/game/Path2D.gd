extends Path2D

onready var follow = $PathFollow2D

#func _process(delta):
	#follow.offset = curve.get_closest_offset(get_global_mouse_position())
	#print(curve.get_closest_offset(get_global_mouse_position()) / curve.get_baked_length())
#	pass

